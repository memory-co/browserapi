{
  "name": "@webmuxd/client",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "webmuxd 的浏览器端 —— 构建产物打进 wheel,不发 npm",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
