{
  "name": "@webmuxd/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "装进被控浏览器的那个扩展 —— 构建产物打进 wheel,不发 npm",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
